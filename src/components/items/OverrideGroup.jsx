import React from 'react';

// The definition is a placeholder. 
// The actual inputs/outputs are generated dynamically in WorkspaceItem.jsx
export const itemDefinition = {
  name: 'Override Group',
  defaultSize: {
    width: 40,
    height: 30,
  },
  inputs: [],
  outputs: [],
};

// This is a visual container, so its body is empty.
export const ItemComponent = () => {
    return null;
};
